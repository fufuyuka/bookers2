<!-- 自分の本の編集ページ -->

<h1>Book edit</h1>

<%= form_with model: @book do |f| %>
<label>Title</label>
  <%= f.text_field :title %>
<label>Body</label>
  <%= f.text_area :body %>
  <%= f.submit 'Update' %>
 <% end %>

<%= link_to "Show", book_path(@book.id) %>
|
<%= link_to "Back", books_path %>